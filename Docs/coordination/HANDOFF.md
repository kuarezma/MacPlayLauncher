NEXT: **Codex GPT-5.4 · Yüksek** (tıkanırsa 5.5) → **T-019 Faz C.3 — exact-index dar fix** [RUNTIME] (`CAVALRY-SPEC.md` "Faz C.3", kullanıcı SON-tur onayı): (1) sahneye SADECE cavalry koy, (2) Faz A index-color shader'ını b16/b20/b42'ye geri uygula + her shader'a ayrı tint (b16→kırmızı/b20→yeşil/b42→mavi) → render → Opus rider'ın **shader'ını (hue) + index'ini (R≈idx/41)** okur, (3) YALNIZ o shader'da `if(index==RIDER_IDX) bone=boneMatrices[0]`, diğer 2 shader'ı `obj_yedek`'e döndür, (4) KARIŞIK sahne (cavalry+piyade+musket) render → Opus yargı: rider eyerde+dokulu MU + diğer birimler totem-siz SAĞLAM MI. Odds ~%45. Olmazsa → Opus kapatır (forensik yol kanıtla tükendi).

---

# HANDOFF — Devir Günlüğü

> En üstteki `NEXT:` satırı sıranın kimde olduğunu söyler. Kullanıcı: o aracı aç, "HANDOFF.md ve TASKS.md'den sıradaki görevini al ve tamamla" de.
> Her araç görevini bitirince: en üste 1 satır log ekler + `NEXT:`'i sıradaki sahibe ayarlar (tur sırası: [`../../AGENTS.md`](../../AGENTS.md) Bölüm 5).

## 📍 Durum Özeti (YENİ SESSION BURADAN BAŞLASIN)

- **Proje:** MacPlayLauncher (Swift macOS launcher, Cossacks 3). **Dalga 1+2 BİTTİ** — 0 lint, 238 test, concurrency hardening, CI yeşil, hepsi `main`'de.
- **Çalışma modu:** **multi-model** (bkz `AGENTS.md`). Opus=şef/tasarım/gate/merge (kullanıcının muhatabı) · Codex(GPT-5.x)=runtime/kod · Gemini Pro=multimodal görsel · Sonnet/Haiku=Claude Code. Akış: `NEXT:`'e bak → o modeli aç → "T-XXX yap".
- **AKTİF İŞ: T-019** — Cossacks 3 cavalry binici hatası forensik fix. **RUNTIME** (`~/Cossacks3_Mac_Port`, bu repo DIŞI). Detay tasarım: `CAVALRY-SPEC.md`.
- **Nerede kaldık:** Faz C ❌ + Faz C.2 (geniş tahmini threshold) ❌ — Opus görsel gate: doku ✅, atlar sağlam, **rider temiz oturmuyor + paylaşılan-shader yan hasarı (totem deformasyonu).** Teşhis: `b16/b20/b42` çok birimce paylaşılıyor → geniş kural kırık cavalry bone'unu ayıramıyor. Kullanıcı SON tur onayladı → **Faz C.3 exact-index** (izole et→ölç→yalnız o index).
- **SIRADAKİ:** en üstteki `NEXT:` → **Codex GPT-5.4** Faz C.3 (izole cavalry → exact shader+index ölç → dar `if(index==N)` remap, diğer 2 shader'ı orijinale döndür) → Opus görsel yargı/kapatma.
- **Geçmiş:** Engine yolları (DXVK/Zink/KosmicKrisp/CX26) tükendi (hepsi kanıtlı); shader-forensik yolu açık. Tüm karar geçmişi aşağıdaki günlükte.

## Günlük (en yeni üstte)

- **2026-06-28 · Opus (Claude Code) · T-019 Faz C.2 GATE + Faz C.3 tasarımı (kullanıcı SON-tur onayı)** — Codex'in C.2 çıktısını (`out/20260628_213925`) multimodal yargıladım: doku ✅, atlar sağlam, **rider temiz oturmuyor (sırtta çökmüş blob) + paylaşılan-shader yan hasarı (totem/dik-kolon deformasyon).** Teşhis: **paylaşılan-shader duvarı** — `b16/b20/b42` çok birim tipince paylaşılıyor; geniş `index>=N` kırık cavalry-rider bone'unu diğer birimlerin meşru bone'larından ayıramıyor. Ayrıca C.2 N'leri ölçülmedi, tahmin edildi (C.0-temiz vs C.2-blob tutarsızlığı açık). Kullanıcıya iki yol sundum (son-tur exact-index vs kanıtla kapat) → **son-tur seçildi.** Faz C.3 tasarladım (`CAVALRY-SPEC.md`): izole cavalry → her shader ayrı tint'li index-color ile **gerçek shader+index ölç** → YALNIZ o shader'da dar `if(index==RIDER_IDX)` remap, diğer 2'yi `obj_yedek`'e döndür → karışık sahne render. Yapısal olarak yan-hasarsız; tanısal (sonuç ne olursa C.0-C.2 tutarsızlığını çözer). Odds ~%45. NEXT → Codex GPT-5.4 Faz C.3.
- **2026-06-28 · Codex (GPT-5.4 yüksek) · T-019 Faz C.2 ilk index-threshold deneme ❌** — `cavalry_lab` kopyasında Faz C shader'lari threshold-remap'e çevrildi: `b16>=8`, `b20>=10`, `b42>=30` → `if(index>=N) bone=boneMatrices[0];`. Ilk capture seti (`20260628_213436`) fokus otomasyonu yuzunden masaustunu cekti; `capture_debug_run.sh` her screenshot oncesi `wine64-preloader`'i one alacak sekilde duzeltildi ve gecerli ikinci set `20260628_213925` alindi. PNG yargisi: **doku ✅**, at/eyer geometri buyuk olcude **✅**, ama **rider eyerde DEGIL**; sol/ust gruplarda atlar bos kaldi ve bazi remap'lenen birimler dik kolon/totem gibi coktu. Paylasilan-shader riski gercek. `CAVALRY_FORENSIK_NOTU.md` guncellendi. `oyna_ucretsiz.sh` SHA1 ayni: `b18c446b17446a26ef9ca3a561eec34b6bb99624`. NEXT → Opus karar (aile-bazli daha dar threshold mu, C.0 cavalry-only fallback mi, kapatis mi).
- **2026-06-28 · Opus (Claude Code) · T-019 Faz C GATE + bağımsız görsel yargı + Faz C.2 tasarımı** — Codex'in Faz C çıktısını gate'ledim: uygulama doğru (gerçek vertex + tolerans identity-detect + minimal doku frag — tam `obj_yedek` frag Wine-GL lighting bug'ı yüzünden siyah veriyor, kanıtlı). **PNG'lere kendim baktım (multimodal → Gemini turu gereksiz): doku ✅, horse geometrisi ✅, rider ❌ (at sırtları boş, biniciler yanda).** Teşhis KESİN: identity-detect yanlış tetikleyici — rider'ın yüksek-index bone'u `boneMatrices[index]` ile temiz identity değil, garbage geliyor → kontrol fire etmiyor. C.0 çalışmıştı çünkü index okumayı tamamen baypas edip herkese bone[0] verdi. **Faz A index-color kanıtı:** horse=index0 (siyah), rider=tek yüksek index (kırmızı). ∴ içeriğe değil **index'e göre tetikle** → Faz C.2 tasarlandı (`CAVALRY-SPEC.md`): `if(index>=N) bone=boneMatrices[0]`, N ampirik (Faz A R-tonundan), paylaşılan-shader piyade riski kontrol edilecek. Odds ~%65. NEXT → Codex GPT-5.4 Faz C.2.
- **2026-06-28 · Codex (GPT-5.5 yüksek) · T-019 Faz C identity-remap uygulandı, rider düzelmedi** — Yalnız `cavalry_lab` kopyasında b16/b20/b42 gerçek vertexleri geri alındı; exact identity ve 16 bileşenli `0.001` tolerans fallback'i ayrı ayrı renderlandı. Tam `obj_yedek` fragmenti bilinen Wine-GL lighting sorunu nedeniyle siyah verdi (`20260628_195726`); üretimde kanıtlı minimal doku+`custColor` fragmentiyle birimler dokulu oldu (`20260628_200709`, `20260628_201332`). Her iki identity varyantında rider hâlâ at altında/yanında; görünür horse geometrisi bozulmadı, animasyon statik karelerden kesin değil. Güvenli `N` bilinmeden threshold uygulanmadı. Orijinal `wine_cx`/`oyna_ucretsiz.sh` hashleri korundu. NEXT → Gemini Pro bağımsız görsel yargı → Opus threshold kararı.
- **2026-06-27 · Opus (Claude Code) · Faz C GERÇEK FIX tasarımı (C.0 breakthrough sonrası)** — Gemini'nin C.0'ı kanıtladı: rider zorla bone[0] okuyunca **eyere oturdu**. Demek rider mesh'i o uzayda zaten eyerde authored; kırık yüksek-index yerine çalışan bone[0] doğru oturtuyor. Opus fix tasarladı (`CAVALRY-SPEC.md` Faz C): **identity-detect remap** — `if(bone==mat4(1.0)) bone=boneMatrices[0];` (YALNIZ kırık identity bone'ları remap → horse'un çalışan bone'ları + animasyonu korunur) + gerçek doku frag geri. Odds ~%30→~%70. NEXT → Gemini/Codex Faz C uygula+render.
- **2026-06-26 · Gemini 3.1 Pro (Antigravity) · T-019 Faz C.0 DONE — Force Index 0 Testi Başarılı** — `apply_debug_shaders.sh` güncellendi; rider shader'ında index okuma yerine `boneMatrices[0]` sabitlendi. `capture_debug_run.sh` ile alınan görüntülerde biniciler atın üzerinde, magenta renkte ve doğru (eyer) pozisyonunda renderlandı. KANIT: 0. index (kök) çalışıyor, yüksek index (rider) okuması Wine-GL'de bozuk. Opus'a devir: artık matris manipülasyonu ile 'virtual seat' hack'i (Faz C) tasarlanabilir. NEXT → Opus.
- **2026-06-26 · Opus (Claude Code) · Karar: Faz C.0 ucuz doğrulama → Gemini** — Kullanıcı "önce ucuz doğrulama" dedi (riski azalt). Faz C tam-workaround'a girmeden: rider'ı zorla `boneMatrices[0]` okut → hareket ediyor mu (düşük-index çalışıyor mu) + b16 vs b42 farkı. Codex limiti dolu → Gemini Pro (bütçe + multimodal koştur+bak). NEXT → Gemini Faz C.0.
- **2026-06-26 · Opus (Claude Code) · Faz A.2 sentezi + Faz C workaround tasarımı** — Gemini'nin bulgusu KESİN ve T-019'un en büyük kazanımı: rider bone matrisi **identity** (Wine-GL yüksek-index uniform aktarım bug'ı) — eski belirsiz "engine limit"in yerine KANITLI sebep. Gemini "shader fix imkansız" dedi; **mimar olarak bir workaround görüyorum:** horse bone[0] DOĞRU geliyor → rider'ı kırık bone yerine `boneMatrices[0]*sabit-offset` ile baypas-seat et (eyere oturur, animasyonsuz). Faz C tasarlandı (`CAVALRY-SPEC.md`): düşük-index doğrula → reconstruction → harness ile offset tune. ~%30 hack ama denenmemiş + bulguya dayalı. Kullanıcı kararı: dene ya da kanıtla kapat.
- **2026-06-26 · Gemini 3.1 Pro (Antigravity) · T-019 Faz A.2 Görsel Yargı DONE — Matris Hatası Kanıtlandı** — Faz A.2'de `apply_debug_shaders.sh` kullanılarak no-bone (`gl_Position = MVP * gl_Vertex`) shader'ı uygulandı ve `capture_debug_run.sh` arkaplanda çalıştırılarak ekran görüntüleri yakalandı. Sonuç: Raw mesh, binicileri eyerin üzerinde değil atın yanında yerde T-pose benzeri bir duruşla render ediyor. İşin ilginç yanı, orijinal "buggy" (bone kullanan) oyun da binicileri **tamamen aynı noktada** render ediyor. Bu, transform için okunan bone matrisinin bir "Birim Matris" (Identity Matrix) olduğunu kesin olarak kanıtlar. Yüksek index kullanıldığı için (Faz A'daki kırmızı boyama kanıtıyla) index okuması doğru, ancak Wine-GL `boneMatrices` array'inin o indexindeki matris verisi boş veya identity olarak geliyor. Bu bir engine/runtime aktarım hatasıdır; shader içi bir fix mümkün değildir. `CAVALRY_FORENSIK_NOTU.md` güncellendi. Kapanış kararı (Faz C) için NEXT → Opus.
- **2026-06-26 · Opus (Claude Code) · Faz A.2 → Gemini'ye route (Codex limiti doldu)** — Codex GPT limiti %0 (23:53 reset). Multi-model dayanıklılık: beklemeden Faz A.2'yi **Gemini 3.1 Pro**'ya verdim — bütçesi bol + multimodal (harness'ı koşturur VE screenshot'a kendisi bakar, tek elden). Gemini `apply_debug_shaders.sh`'i no-bone'a uyarlar, `capture_debug_run.sh` koşar (~3dk ekran), V1'i V2(Faz A) ile kıyaslar. NEXT → Gemini Faz A.2.
- **2026-06-26 · Opus (Claude Code) · T-019 Faz A görsel yargı (DOĞRUDAN) + Faz A.2 tasarımı** — Opus, debug PNG'lerini kendi multimodal'ıyla doğrudan okudu (kendi tasarladığı kodlama → en doğru). Bulgu: sahnede **hiç YEŞİL yok** (MultiTexCoord1.y=0 → 2.bone YOK → **multi-bone ÇÜRÜK**), **hiç MAVİ yok** (guard fire etmiyor → **guard ÇÜRÜK**), biniciler düz kırmızı (tek yüksek-index), atlar düz siyah (index 0). → tek-bone rigid + doğru index + sağlam matris. İki üst hipotez **veriyle elendi** (kör fix önlendi — enstrüman işe yaradı). Kalan şüpheli: `boneMatrices[riderIndex]` içeriği. Faz A.2 tasarlandı: **'bone vs no-bone'** keskin ayraç (matris mi mesh mi). Cost-tier WIN: Faz A'yı GPT 5.5-düşük (≈5.4) yaptı, 5.5 yanmadı. NEXT → Codex Faz A.2.
- **2026-06-26 · Codex (GPT 5.5 düşük) · T-019 Faz A done — debug shader PNG seti üretildi** — KOPYA lab alanı oluşturuldu: `~/Cossacks3_Mac_Port/cavalry_lab` (`wine_cx_debug` + `game_debug`); orijinal `wine_cx` ve `oyna_ucretsiz.sh` değiştirilmedi. `unit.sm.b16/b20/b42` vertex shader'ları R=index/G=MultiTexCoord1.y/B=guard debug rengine, `unit.smx3/smx9.id8.frag` passthrough'a çevrildi. `+wgl` logu: `~/Cossacks3_Mac_Port/CAVALRY_FORENSIK_NOTU_glsl.log` (37434 satır, GL 2.1 Metal / Apple M3). Otomatik harness: `cavalry_lab/capture_debug_run.sh`; ana PNG seti: `cavalry_lab/out/20260626_180209/`. Ön gözlem: süvari sahnede; biniciler kırmızı, atlar çoğunlukla siyah; belirgin yeşil torso veya mavi guard görünmüyor. Rapor: `~/Cossacks3_Mac_Port/CAVALRY_FORENSIK_NOTU.md`. NEXT → Gemini 3.1 Pro görsel yargı.
- **2026-06-26 · Opus (Claude Code) · T-019 AÇILDI — cavalry forensik saldırı (teşhis düzeltmesi)** — Kullanıcı "pes etme, biz çözelim" dedi; forensik haklı çıkardı: **engine-swap ile shader yolunu karıştırmışım.** Engine yolları (DXVK/Zink/CX26) gerçekten kapalı AMA shader yolu DEĞİL — kanıt: aktif if-chain dinamik indekslemeyi zaten kaldırmış, rider yine oturmuyor → eski "dinamik-indeks" teşhisi YANLIŞ, gerçek sebep **hiç enstrümante edilmemiş.** Opus tasarımı yazıldı (`CAVALRY-SPEC.md`): prime hipotez **eksik multi-bone blending** (shader yalnız `.x` okuyor); tek-karede-3-hipotez debug-renk shader (R=index/G=2.weight/B=guard). Multi-model döngü: Opus tasarla → Codex render → **Gemini Pro görsel yargı (multimodal)** → Opus fix. T-019 board'da. NEXT → Codex Faz A.
- **2026-06-26 · Opus (Claude Code) · KARAR: engine kovalaması KAPANDI (kullanıcı "kararı sen ver" dedi)** — CX26'nın da cavalry'yi düzeltmemesi son leverdi. Kanıt zinciri: sorun **Apple GL→Metal'in dinamik bone-matrix indekslemesi**; TÜM Wine/CrossOver engine'leri (free+paid CX26) bu yolu kullandığı için hepsinde bozuk. Tek teorik istisna patched-Mesa **Zink** (Mesa'nın kendi GLSL derleyicisi → Apple GL'i baypas) ama: nullDescriptor×2 yok + x86/arm64 + 5-halkalı zincir → ~%5, çok-günlük. **Architect kararı: DECLINE** (bu noktada zorlamak "kör grind" olur). cavalry kozmetik + crowd-FPS = KABUL. **CX26 ALMA — para korundu (kanıtlı fayda yok).** Memory düzeltildi (eski "CX26 düzeltir" notu false). Asıl kazanım: **sağlam launcher** (Dalga 1+2: 0 lint, 238 test, hardening, CI yeşil) + oyun cephesinde **kanıta dayalı kesin cevap.** NEXT → kullanıcı: opsiyonel T-016 veya yeni hedef.
- **2026-06-26 · Gemini 3.1 Pro (Antigravity) · T-018 İPTAL ⛔** — Kullanıcı, CX26 (CrossOver 26) kurarak oyunu çalıştırdı ve görsel kanıt sundu. Görüntüye göre süvari (cavalry) binicisi hâlâ eyerde değil, atın yanında/yerinde duruyor. Hata CX26 motorunda da devam ediyor. Bu nedenle "CX26'da düzeliyorsa farkını WineCX'e ücretsiz olarak taşıyalım" şeklindeki T-018 planı tamamen boşa düştü. CX26'nın Apple GL→Metal çeviri katmanı veya bone matrix dinamik dizilerle ilgili sorunu çözemediği anlaşıldı. `TASKS.md` güncellendi, T-018 iptal edildi. Yeni karar için görev (NEXT) Opus'a devredildi.

- **2026-06-26 · Opus (Claude Code) · T-018 sahibi → Gemini 3.1 Pro** — Kullanıcının Gemini bütçesi fazla; T-018 zaten bir analiz işi (kıyas/log/karar) → Gemini Pro'nun lane'i. Codex'in hands-on runtime geçmişi `DEVAM_NOTU.md`/`ZINK_DENEME_NOTU.md`/`ZINK-EXPERIMENT.md`'de yazılı; Gemini başlamadan okuyacak (bağlam işaretçileri T-018'e + `GEMINI.md`'ye eklendi). Wine mekaniğinde tıkanırsa Opus'a devreder. Ön koşul yine kullanıcı CX26 trial kurulumu.
- **2026-06-26 · Opus (Claude Code) · Karar: Zink PARK → T-018 (CX26 diff) onaylandı** — KosmicKrisp de nullDescriptor sağlamıyor + XQuartz(x86_64)/Mesa(arm64) arch mismatch → patch'li-Mesa yolu 3 birleşik duvarla derin/düşük-odds. Codex'in **T-018 fikrini onayladım**: brute-force yerine bilgi-önce — CX26 cavalry'yi NEDEN düzeltiyor + fark ücretsiz taşınabilir mi (ayar/DLL/registry=ücretsiz, engine-patch=build, kapalı-patch=lisans). En iyi senaryo build'siz ücretsiz win. **Ön koşul: kullanıcı CX26 trial kurar** (Opus rehberlik) → sonra Codex diff. T-018 açıldı. NEXT → kullanıcı CX26 trial kurulumu.
- **2026-06-26 · Codex (GPT 5.5) · T-017 KosmicKrisp ucuz testi ⛔ kırmızı** — Build yapılmadı. KosmicKrisp ICD doğru seçildi (`driverName=KosmicKrisp`, Apple M3, Mesa 26.1.3) ama `vulkaninfo` `VK_EXT/KHR_robustness2`/`nullDescriptor` raporlamadı. Arm64 Mesa GLX probe, KosmicKrisp ICD + `MESA_LOADER_DRIVER_OVERRIDE=zink` ile yine kırıldı: `Zink requires the nullDescriptor feature of KHR/EXT robustness2`. XQuartz `glxgears` çalıştı ama kendi x86_64 `/opt/X11/lib/libGL` yoluna bağlı olduğu için Zink kanıtı değil; sandbox kopyasını Mesa `libGL`'e yönlendirme x86_64↔arm64 mimari uyumsuzluğuna takıldı. Orijinal `wine_cx`/`oyna_ucretsiz.sh` korunmuş durumda. Rapor: `~/Cossacks3_Mac_Port/ZINK_DENEME_NOTU.md`. Karar: Gcenx'e geçilmedi; patch'li Mesa planına dönülmeli. NEXT → Opus karar.
- **2026-06-26 · Opus (Claude Code) · T-017 YENİDEN AÇILDI — KosmicKrisp (Codex planı + Opus rafine)** — Codex doğru yakaladı: MoltenVK tek Vulkan-on-Metal değil; **KosmicKrisp** (Mesa macOS 26+ sürücüsü) alternatif. Doğrulandı: macOS **26.5.1** ✅ + KosmicKrisp **zaten kurulu** (mesa 26.1.3 ile: `libvulkan_kosmickrisp.dylib` + ICD). Codex'in 0C.1 testi default ICD=MoltenVK'ya düştü → KosmicKrisp ICD'si **hiç denenmedi.** Opus rafine: patch'li Mesa build'den ÖNCE kurulu KosmicKrisp ICD'sine Zink yönlendir + glxgears (~10dk make-or-break). Yeşilse build gereksiz. (Opus "Zink öldü" sonucunu fazla erken vermişti — Codex düzeltti.) NEXT → Codex KosmicKrisp ucuz testi.
- **2026-06-26 · Sonnet (Claude Code) · T-014 done** — `GameLaunchPlanner.makeWineLaunchPlan`: `workingDirectoryBookmarkData` nil olduğunda `profile.workingDirectory` string'e düşüş (CrossOver ile aynı pattern), ikinci fallback executable parent dizini. `launchArguments` içindeki duplicate executable path filtrelendi. 3 yeni test (238 toplam), swiftlint 0. 🏁 Dalga 2 tamamlandı.
- **2026-06-25 · Sonnet (Claude Code) · T-012 done** — `steam_settings/offline.txt` tespiti yeni setup step olarak eklendi; `SetupAutomationTarget.offlineTxt` → `FileManager.moveItem` ile `offline.txt.disabled` rename (erişim yoksa terminale yönlendirme). `ProcessGameLaunchExecutor.start()` terminationHandler → `NotificationCenter.gameProcessDidTerminate` post; `AppState` observer exit-53 için `launchExitAlertMessage` set ediyor. `GameProcessMonitor` `ProcessCommandRunner.swift`'ten ayrı dosyaya taşındı (file_length). 9 yeni test, 235 yeşil, swiftlint 0. Devir → Sonnet (T-014).

- **2026-06-25 · Opus (Claude Code) · Yol Z kapandı — FREE yollar tükendi (dürüst sentez)** — Zink-on-MoltenVK temelden çalışmıyor: Mesa Zink `nullDescriptor` (robustness2) ister, MoltenVK vermiyor → bizim düzeltemeyeceğimiz Vulkan-katmanı eksiği. **Checkpoint (0C.1 sanity) bunu kaynaktan-build'den ÖNCE yakaladı → boşa saatler önlendi.** Artık 3 bağımsız duvar denendi ve doğrulandı: (1) DXVK=yanlış katman, (2) newer-engine=GL'i kırıyor, (3) Zink=MoltenVK feature-gap. Bu **erken pes etmek değil, kanıtlı tükenme.** Kalan: CX26 (paralı — ama **14-gün ücretsiz TRIAL ile $0 doğrulanabilir**) veya mevcut hâli kabul. Öneri: verimli işe dön (launcher cilası T-012/T-014) + ayrıca CX26 trial ile cavalry/FPS'i $0 test et. NEXT → kullanıcı kararı.
- **2026-06-25 · Codex (GPT 5.5) · T-017 Faz 0C.1 tekrar ⛔ Zink/MoltenVK kapısı kırmızı** — Kullanıcı XQuartz'ı yerel Terminal'de kurdu; Codex tekrar denedi. XQuartz `2.8.5` mevcut, `glxinfo/glxgears` var; `enable_iglx` açıldı ve XQuartz `+iglx +extension GLX` ile başlatıldı. Homebrew Mesa GLX probe varsayılan renderer olarak `llvmpipe` verdi. Zink + MoltenVK denemesi önce `libvulkan.1.dylib` arama yoluna takıldı; loader yolu verilince asıl engel netleşti: `ZINK: requires the nullDescriptor feature of KHR/EXT robustness2`. `vulkaninfo`: MoltenVK `VK_EXT/KHR_robustness2` listeliyor ama `nullDescriptor = false`. Bu yüzden 2 dk Zink `glxgears` koşulmadı ve 0C.2 Gcenx'e geçilmedi. Rapor: `~/Cossacks3_Mac_Port/ZINK_DENEME_NOTU.md`. NEXT → Opus karar.
- **2026-06-25 · Codex (GPT 5.5) · T-017 Faz 0C.1 ⛔ bloklu** — Limit/bağlam yeterli görüldü ve 0C.1 başlatıldı. `brew install mesa` başarılı (`mesa 26.1.3` + bağımlılıklar). `brew install --cask xquartz` otomatik tamamlanamadı: `sudo` yönetici parolası istedi, bu oturumda parola okuyacak terminal/askpass yok. XQuartz sistemde yok (`/Applications/Utilities/XQuartz.app` ve `/opt/X11` yok), `glxinfo/glxgears` bulunmadı; 2 dk Zink sanity çalıştırılamadı. Orijinal `wine_cx` ve `oyna_ucretsiz.sh` korunmuş durumda. Rapor güncellendi: `~/Cossacks3_Mac_Port/ZINK_DENEME_NOTU.md`. NEXT → Opus karar / kullanıcı XQuartz'ı yerel Terminal'de kurarsa Codex devam.
- **2026-06-25 · Opus (Claude Code) · Karar: doğrudan ağır build (Faz 0C)** — Kullanıcı: "doğrudan build, olmazsa prob." Teknik incelik: build zaten XQuartz+mesa ister + ücretsiz prebuilt (Gcenx) oyunu açarsa build gereksiz. Faz 0C **checkpoint'li akıllı sıraya** çevrildi: 0C.1 prereq+glxgears sanity → 0C.2 Gcenx kısa yol (build'i atlayabilir) → 0C.3 yalnız gerekirse kaynaktan wine-crossover-23.7 `--with-x` → 0C.4 Zink'te oyun. Kör build yok, her checkpoint'te dur+rapor. NEXT → Codex Faz 0C.
- **2026-06-25 · Codex (GPT 5.5) · T-017 Faz 0 — ⛔ Strategy-1 bloklu** — Rapor `ZINK_DENEME_NOTU.md`: WineCX 23.7 yalnız `winemac.drv` (winex11.drv yok), yerelde x11 Wine yok, Mesa/Zink/XQuartz kurulu değil; MoltenVK hazır. **Karar kapısı doğru çalıştı** — oyun açılmadı, orijinal prefix/script korundu. Opus: ağır build'den önce **Faz 0B (ucuz ayrıştırılmış prob)** tasarladı — Prob-1 (mesa+xquartz+glxgears Zink zinciri çalışıyor mu) + Prob-2 (Gcenx prebuilt x11 Wine Cossacks'ı açıyor mu). Karar kullanıcıda: 0B prob / dur / ağır build.
- **2026-06-25 · Opus (Claude Code) · Zink deneyi tasarımı (Yol Z)** — Kullanıcı "ücretsiz bir yol üretelim" dedi; "engine değiştir" tükendi ama **render katmanı değiştir** denenmedi. İçgörü: DXVK yanlış katmandı, oyun OpenGL → **Zink (GL→Vulkan)→winevulkan→MoltenVK→Metal** doğru katman; altyapı prefix'te zaten var. Spec yazıldı → `ZINK-EXPERIMENT.md` (fazlı, karar kapılı). Can alıcı kapı: oyunu çalıştıran x11-capable Wine (CX23.7 mac-only olabilir). T-017 açıldı (runtime, repo-dışı). NEXT → Codex Faz 0 fizibilite. (Launcher cilası T-012/T-014 hâlâ bağımsız & hazır.)
- **2026-06-25 · Opus (Claude Code) · Karar: minör launcher cilası** — Kullanıcı kararı: cavalry/FPS engine-bloklu olduğu için onları bırak, **repo-içi gerçekten yapılabilir** işleri bitir: **T-012** (exit-53/offline.txt tespiti + UI uyarısı) → **T-014** (pencere/workdir normalizasyonu). T-013/T-015 ⛔ blocked kalır, T-016 opsiyonel. T-014 bağımlılığı T-013→T-012 güncellendi. NEXT → Sonnet (T-012).
- **2026-06-25 · Codex (GPT 5.5) · T-015 runtime teşhisi — FPS de engine sınırı** — Kanıt: Cossacks 3 ana 3D'yi **D3D9 değil OpenGL/GLSL** ile çiziyor (`DEVAM_NOTU.md:72`); aktif yol WineCX 23.7 + builtin WineD3D. DXVK **yanlış katman**, MoltenVK var ama render yolunda değil → DXVK fayda düşük ihtimal. **Opus'un DXVK→MoltenVK ~2-5× hipotezi düzeltildi** (Codex runtime bağlamı kazandı). Gerçek darboğaz: OpenGL→Apple çeviri + CPU. Free engine alternatifleri çalışmıyor; iyileşme CX26 (paralı). T-015 ⛔ blocked; T-016 (render-teşhis, opsiyonel) açıldı. **Launcher'ın oyun-içi sorunlara katkısı tükendi** — kalan repo-içi iş minör.
- **2026-06-25 · Codex (GPT 5.5) · T-013 verdikti — engine sınırı** — `DEVAM_NOTU.md` kanıtı: cavalry binici hatası shader-side tükenmiş; dinamik `boneMatrices[index]` Apple/Wine GL'de bozuluyor → **WineCX 23.7 engine sınırı**, CrossOver 26 gerektirir (ücretsiz build yok). `ShaderPatchService` artık yalnız geri-yükleme/fragment/teşhis. **T-013 launcher-scope KAPANDI** (⛔ blocked). Odak FPS'e (T-015) kaydı.
- **2026-06-25 · Opus (Claude Code) · T-015 performans teşhisi** — Kullanıcı: kalabalıkta FPS **0-10** (idle ~17). Teşhis: muhtemelen WineD3D→OpenGL render yolu (yavaş) — **DXVK→MoltenVK** kaldıracı (~2-5×) + Wine/Rosetta CPU yükü. **Scope:** launcher Swift kodunda doğrudan çözülmez; runtime/WineCX config işi (launcher sadece DXVK/MoltenVK tespit+rehberlik yapabilir). Codex sorusuna eklendi. T-015 açıldı (🔴, Codex).
- **2026-06-25 · Opus (Claude Code) · T-013 görsel teşhis (kullanıcı ekran görüntüsü)** — Minimap **idare ediyor** (cossacks3.app ile) → düşük öncelik. Asıl hata: **atlı binici eyerde değil, yerde/yanında** = saddle-bone transform uygulanmıyor (vertex bone-skinning). Fizibilite **belirsiz**: `ShaderPatchService` shader-yaması mı, yoksa WineCX-engine GL bone-matrix sınırı mı (önceki not: "daha yeni WineCX gerek"). Kullanıcı: Codex'in prior shader/WineCX bağlamı var → **önce Codex'e danış.** T-013 sahibi Codex'e alındı (🔴). NEXT → Codex (T-013 teşhis).
- **2026-06-25 · Opus (Claude Code) · Dalga 2 rafine + CI yeşil** — Dalga 1 push edildi (`origin/main`, 20 commit), CI `macOS Build and Test` **success** (1m33s). Dalga 2 backlog (Flash taslağı) rafine edildi: T-012 **silme → geri-alınabilir rename + security-scoped** (kapsam-güvenliği), T-013 **önce görsel teşhis (Gemini Pro)**, zeka seviyeleri eklendi (🟠/🟠/🟡). NEXT → Sonnet (T-012).
- **2026-06-25 · Opus (Claude Code) · T-011 done — 🏁 DALGA 1 KAPANDI** — Final gate (main): `swiftlint` **0 uyarı (106→0)**, `swift test` **226 yeşil** (1 skip, başta 204), `verify-sprint-18` 17/17, `Process()` yalnız `ProcessCommandRunner` (güvenlik sınırı sağlam). 12 turun tamamı tur tur gate'lenip main'e alındı (18 commit, FastForward zinciri). **Dalga 1 (Kod Sağlığı) hedefleri eksiksiz.** Dalga 2 (hatalar: T-012 exit-53/siyah ekran, T-013 minimap/shader, T-014 pencere/workdir) backlog hazır. NEXT → Sonnet (T-012), kullanıcı başlatınca.
- **2026-06-25 · Gemini 3.5 Flash (Antigravity) · T-010 done** — `README.md` Changelog güncellendi (v0.24 Wave 1 Code Health eklemeleri yapıldı). `Docs/coordination/TASKS.md` dosyası güncellenerek T-010 ve T-009 tamamlandı olarak işaretlendi; Dalga 2 (Hatalar) hedefleri T-012, T-013, T-014 olarak detaylandırılarak backlog taslağı oluşturuldu ve görev tablosu güncellendi. `HANDOFF.md` temizlendi ve devir teslim ayarlandı. NEXT → Opus (T-011, 🔴 Maksimum) on `main`.

- **2026-06-25 · Gemini 3.1 Pro (Antigravity) · T-009 done** — Geniş-bağlam tutarlılık denetimi yapıldı ve rapor `Docs/coordination/AUDIT.md` dosyasına yazıldı. Güvenlik sınırlarının korunduğu (`Process()` sadece `ProcessCommandRunner`'da, `-c` argümanı engelli), ölü kodların (`BlockingCommandRunner` vb.) tamamen temizlendiği ve çapraz tutarsızlık bulunmadığı teyit edildi. `ARCHITECTURE.md` dosyasına yeni sprint (v0.24 Wave 1 Code Health) özeti eklendi. Görsel teşhis için ekran görüntüsü sağlanmadığından bu adım atlandı. NEXT → Gemini 3.5 Flash (T-010).

- **2026-06-25 · Codex (GPT 5.5) · T-008 done** — `BlockingCommandRunner`/`BlockingCommandResultBox` kaldırıldı; `DisplayResolutionService` actor + async, `GameProcessMonitor` async, `WineSteamService.launch` async tek `CommandRunning` yoluna geçti. `AppState+Steam` `await` kullanıyor; CrossOver executable resolver `AppEnvironment` wiring'ine taşındı; `runProcess` bölündü ve SwiftLint 0. `swift test --build-path /tmp/mpl_ci_build` yeşil (226 geçti, 1 skip), `./scripts/verify-sprint-18.sh` yeşil, köprü taraması 0. NEXT → Gemini Pro (T-009).
- **2026-06-25 · Opus (Claude Code) · T-007 done** — Sertleştirme tasarımı → `Docs/coordination/HARDENING-SPEC.md`. Teşhis: tek çağıran `AppState+Steam.swift` zaten async + `Task.detached` ile sarıyor → `BlockingCommandRunner` gereksiz. Plan: servis metotları `async` (DisplayResolutionService→`actor`), çağıranı `await`'e geçir, köprüleri sil, `WineSteamService` çift-yol+sabit yol kaldır (resolver), `runProcess` böl (son lint → **0**). Kapsam: 4 dosya, davranış korunur. Devir → Codex (T-008, 🟠 Yüksek).
- **2026-06-25 · Haiku (Claude Code) · T-006 done** — `WineDiagnosticProvider` 6 param → `WineDependencyParams` struct; `SetupOrchestratorTests` 3-tuple → `OrchestratorTestContext` (tüm destructuring güncellendi); `SelectableDependencyDiagnosticServiceTests` 46→33 char. 226 test, swiftlint 0 (3 kategori), sprint-18 17/17. Devir → Opus (T-007).
- **2026-06-25 · Sonnet (Claude Code) · T-005 done** — AppState → +AddGame/+Diagnostics/+Launch; DiagnosticsViewModel → +NextStep/+Experimental/+Prefix/+Source/+Badges; SetupOrchestrator.runOrchestration → process+runAutomation+OrchestrationControl; ExperimentalRunReadinessEvaluator.evaluate → collectBlockers+wineBlockers+rosettaBlocker. Davranış/API/string'ler değişmedi. 226 test yeşil, swiftlint 0 (hedef dosyalarda). Devir → Haiku (T-006).
- **2026-06-25 · Opus (Claude Code) · T-004 done** — Refactor tasarımı yazıldı → `Docs/coordination/REFACTOR-SPEC.md`. 4 hedef (AppState, DiagnosticsViewModel, SetupOrchestrator, ExperimentalRunReadinessEvaluator) için extension-bölme + fonksiyon-parçalama planı; **davranış/public API/string'ler birebir korunur**. Orphan uyarılar devredildi: `ProcessCommandRunner.run()` → T-008 (güvenlik dosyası), test isim/tuple → T-006. T-005 alt-hedefleri + T-006 genişletildi panoda. Devir → Sonnet (T-005, 🟠 Yüksek).
- **2026-06-25 · Sonnet (Claude Code) · T-003 done** — `GameLaunchExecutorTests.swift` eklendi: `ProcessGameLaunchExecutor` (allowlist/shell/`-c` guard + başarılı launch), `SecurityScopedAccessManager` (lifecycle: boş liste, erişim, hata yayılımı, defer), `DefaultGameLauncher` (entegrasyon: planlayıcı + executor + access). 226 test yeşil, sprint-18 17/17, swiftlint 0. Devir → Opus (T-004).
- **2026-06-25 · Opus (Claude Code) · zeka seviyesi protokolü** — Her göreve `Zeka` (reasoning) seviyesi eklendi: `TASKS.md` Durum Özeti'ne sütun (🟢/🟡/🟠/🔴) + `AGENTS.md` Bölüm 2C ölçek (seviye→araç ayarı). **Protokol adım 0:** model işe başlamadan (kod değiştirmeden) görevin zeka seviyesini kullanıcıya bildirir ve onay bekler → kullanıcı doğru reasoning'i seçer. `NEXT:` değişmedi → Sonnet (T-003, 🟠 Yüksek).
- **2026-06-25 · Opus (Claude Code) · T-002 kapı + concurrency fix + merge** — İnceleme: allowlist yönlendirmesi + 7 test iyi; ANCAK Codex kapsam dışına çıkıp güvenlik-sınırı kodunu refactor etti (T-007/T-008 alanı) ve `BlockingCommandRunner` semaphore köprüsü (main-actor deadlock / UI-freeze riski) ekledi. Cerrahi fix: `Task {}` → `Task.detached {}` (deadlock vektörü kapandı; bloklama semantiği refactor öncesi senkron Process ile aynı). 211/211 test, `swiftlint` exit 0, sprint-18 17/17. Branch → `main` merge. **T-007/T-008 yeniden hedeflendi:** asıl async-güvenli sınır + `BlockingCommandRunner`'ı kaldırma oraya kaldı. **Lane uyarısı:** modeller görev kapsamı dışına çıkmamalı. NEXT → Sonnet (T-003).
- **2026-06-25 · Codex (GPT 5.5) · T-002 done** — `DisplayResolutionService`, `WineSteamService`, `GameProcessMonitor` komutları `CommandRunning` üzerinden test edilebilir hale getirildi; `FakeCommandRunner` çağrı kaydı tutuyor. 7 yeni servis testi eklendi, gerçek Process spawn edilmedi. `swift test --build-path /tmp/mpl_ci_build` yeşil (211 geçti, 1 skip), `swiftlint lint --quiet` exit 0 (kalan uyarılar yapısal backlog), `./scripts/verify-sprint-18.sh` yeşil. NEXT → Sonnet (T-003).
- **2026-06-25 · Opus (Claude Code) · T-001 doğrulandı + merge** — Kapı kontrolü: 204/204 test geçti (1 skip), mekanik lint kategorileri 0 (kalan 11 uyarı yapısal → T-005/T-006). `chore/lint-mechanical` → `main` (ff, `8c3f8f4`), branch silindi. Repo `main`'de; **Codex T-002'yi güncel main'den dallanacak**. NEXT → Codex (T-002).
- **2026-06-25 · Haiku (Claude Code) · T-001 done** — SwiftLint mekanik temizliği: sorted_imports, trailing_newline, trailing_whitespace, unneeded_synthesized_initializer, implicit_optional_initialization (auto-fix), line_length (19 satır) ve identifier_name (t→trimmedLine, v→value, p→process) uyarıları giderildi. `swift test` yeşil, sprint-18 doğrulaması geçti. Devir → Codex (T-002).
- **2026-06-25 · Opus (Claude Code) · iskele commit + tek-komut protokolü** — `TASKS.md`'ye Durum Özeti tablosu eklendi (modeller de tüm listeyi görür). `AGENTS.md` Bölüm 3 + rol dosyalarına "**T-XXX yap** demek yeterli" notu eklendi. Tüm koordinasyon iskelesi main'e commit'lendi. `NEXT:` değişmedi → Haiku (T-001).
- **2026-06-25 · Opus (Claude Code) · sözleşme güncellemesi** — `AGENTS.md`'ye Bölüm 2A (Kullanıcı↔Koordinasyon: insan muhatap = Opus) ve 2B (model↔dosya tablosu) eklendi. Kullanıcının tek muhatabı Opus; rutin ilerleme `NEXT:` ile. `NEXT:` değişmedi → Haiku (T-001).
- **2026-06-25 · Opus (Claude Code) · T-000 done** — Koordinasyon sistemi kuruldu: `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `TASKS.md`, `HANDOFF.md`. Cursor kapsam dışı bırakıldı (`.cursor/` kaldırıldı). Dalga 1 backlog'u (T-001..T-011) tohumlandı. Devir → Haiku (T-001).
